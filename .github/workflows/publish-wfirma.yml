name: Publish n8n-nodes-faktoapp-wfirma

on:
  push:
    tags:
      - 'wfirma-v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint
        working-directory: packages/n8n-nodes-wfirma

      - name: Build
        run: npm run build
        working-directory: packages/n8n-nodes-wfirma

      - name: Test
        run: npm test
        working-directory: packages/n8n-nodes-wfirma

      - name: Publish with provenance
        run: npm publish --provenance --access public
        working-directory: packages/n8n-nodes-wfirma
